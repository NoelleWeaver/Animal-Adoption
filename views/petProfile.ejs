<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/ionicons@5.5.2/dist/css/ionicons.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/profileStyle.css">
    <title><%= pet.name %>'s Page</title>
</head>
<body>
    <a href="/"><i class="fa-solid fa-arrow-left"></i></a>
    <div class="img-container">
        <h1 class="name"><%= pet.name %></h1>
        <img src="<%= pet.imageURL %>" alt="<%= pet.name %>">
        <div class="grid">
            <div class="container1">
                <p class="label2">Breed</p>
                <p class="breed"> <%= pet.breed %></p>
            </div>
            <div class="container2">
                <p class="label2">Age</p>
                <p class="age"> <%= pet.age %> years</p>
            </div>
            <div class="container3">
                <p class="label2">Gender</p>
                <p class="gender"> <%= pet.gender %></p>
            </div>
            <div class="container4">
                <p class="label2">Location</p>
                <p class="city"> <%= pet.city %></p>
                <p class="state"> <%= pet.state %></p>
            </div>
        </div>
    </div>
    <div class="container">
        <p class="label">Description</p>
        <p class="description"><%= pet.description %></p>
        <p class="label">Behavior</p>
        <p class="behavior"><%= pet.behavior %></p>
        <p class="label">History</p>
        <p class="history"><%= pet.history %></p>
    </div>
</body>
</html>